#include <stdio.h>

int main()
{
    char c[] = "This is a c string, it is an array of individual characters";
    int i; //This is an integer
    i = 0;

    printf("%s\n", c); //%s indicates a string, %d an integer, and %f a float or double
    printf("i equals = %d\n", i); // \n indicates "form a new line"
    return i;
}
